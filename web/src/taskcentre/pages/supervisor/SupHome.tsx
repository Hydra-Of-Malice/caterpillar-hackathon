/**
 * Route alias for `/tc/sup`. The screen itself lives in `Dashboard.tsx`; this file exists because
 * the Task Centre router imports the supervisor home as `SupHome`.
 */
export { default } from './Dashboard';
